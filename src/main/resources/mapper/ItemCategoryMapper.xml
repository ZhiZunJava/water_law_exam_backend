<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.can.water_law_exam_backend.mapper.ItemCategoryMapper">

    <resultMap id="BaseResultMap" type="org.can.water_law_exam_backend.entity.ItemCategory">
        <id column="id" property="id"/>
        <result column="title" property="title"/>
        <result column="parent_id" property="parentId"/>
        <result column="is_leaf" property="isLeaf"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, title, parent_id, is_leaf, create_time, update_time
    </sql>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM tb_item_category
        ORDER BY parent_id ASC, sort_order ASC, id ASC
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM tb_item_category
        WHERE id = #{id}
    </select>

    <select id="selectByParentId" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM tb_item_category
        WHERE parent_id = #{parentId}
        ORDER BY sort_order ASC, id ASC
    </select>

    <select id="countChildren" resultType="int">
        SELECT COUNT(*)
        FROM tb_item_category
        WHERE parent_id = #{id}
    </select>

    <select id="countItemsByCategoryId" resultType="int">
        SELECT COUNT(*)
        FROM tb_item_bank
        WHERE category_id = #{categoryId}
    </select>

    <insert id="insert" parameterType="org.can.water_law_exam_backend.entity.ItemCategory"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO tb_item_category (title, parent_id, is_leaf, sort_order)
        VALUES (#{title}, #{parentId}, #{isLeaf}, 0)
    </insert>

    <update id="update">
        UPDATE tb_item_category
        SET title = #{title},
            parent_id = #{parentId},
            is_leaf = #{isLeaf},
            update_time = NOW()
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM tb_item_category
        WHERE id = #{id}
    </delete>

    <delete id="deleteBatch">
        DELETE FROM tb_item_category
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

</mapper>


