<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.can.water_law_exam_backend.mapper.PapersTemplateMapper">
    <sql id="Base_Column_List">
        id,
        template_name,
        papers_count,
        max_score,
        key_proportion,
        create_time,
        update_time
    </sql>
    <resultMap id="TemplateResultMap" type="org.can.water_law_exam_backend.entity.PapersTemplate">
        <id property="id" column="id"/>
        <result property="templateName" column="template_name"/>
        <result property="papersCount" column="papers_count"/>
        <result property="maxScore" column="max_score"/>
        <result property="keyProportion" column="key_proportion"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <insert id="insert" parameterType="org.can.water_law_exam_backend.entity.PapersTemplate" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO tb_papers_template (template_name, papers_count, max_score, key_proportion)
        VALUES (#{templateName}, #{papersCount}, #{maxScore}, #{keyProportion})
    </insert>

    <update id="update" parameterType="org.can.water_law_exam_backend.entity.PapersTemplate">
        UPDATE tb_papers_template
        SET template_name = #{templateName},
            papers_count = #{papersCount},
            max_score = #{maxScore},
            key_proportion = #{keyProportion},
            update_time = NOW()
        WHERE id = #{id}
    </update>

    <delete id="deleteBatch">
        DELETE FROM tb_papers_template WHERE id IN
        <foreach collection="ids" item="id" open="(" close=")" separator=",">#{id}</foreach>
    </delete>

    <select id="selectById" resultMap="TemplateResultMap">
        SELECT id, template_name, papers_count, max_score, key_proportion, create_time, update_time
        FROM tb_papers_template
        WHERE id = #{id}
    </select>

    <select id="selectByPage" resultMap="TemplateResultMap">
        SELECT id, template_name, papers_count, max_score, key_proportion, create_time, update_time
        FROM tb_papers_template
        <where>
            <if test="key != null and key != ''">
                AND template_name LIKE CONCAT('%', #{key}, '%')
            </if>
        </where>
        ORDER BY id DESC
    </select>

    <select id="selectByTemplateName" resultMap="TemplateResultMap">
        select
        <include refid="Base_Column_List"/>
        from tb_papers_template
        where template_name=#{templateName}
    </select>
</mapper>
